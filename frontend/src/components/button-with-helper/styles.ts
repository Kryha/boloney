import styled from "@emotion/styled";
import { color, margins } from "../../design";
import { Row } from "../atoms";
import { Info } from "../buttons/styles";

export const ButtonWithHelperWrapper = styled(Row)`
  gap: ${margins.small0};
`;
