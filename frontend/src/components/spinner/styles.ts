import styled from "@emotion/styled";

export const SausageContainer = styled.div`
  width: 100px;
  height: 100px;
  svg {
    width: 100%;
    height: 100%;
    visibility: hidden;
    overflow: visible;
  }
  path {
    fill: none;
    stroke-linecap: round;
  }
`;
