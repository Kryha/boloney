import styled from "@emotion/styled";
import { BaseColumn, VolumeBlock } from "../../atoms";

export const VolumeSliderContainer = styled(VolumeBlock)`
  position: absolute;
  right: 0px;
`;
export const VolumeSliderWrapper = styled(BaseColumn)`
  height: 100%;
  min-height: 25px;
`;
