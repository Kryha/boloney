import styled from "@emotion/styled";
import { InformationBlock } from "../../atoms";

export const RulesWrapper = styled(InformationBlock)`
  position: absolute;
  right: 0;
  overflow-y: scroll;
`;
