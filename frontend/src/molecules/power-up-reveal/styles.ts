import styled from "@emotion/styled";
import { BaseRow } from "../../atoms";
import { POWER_UPS_INSIGHT_WIDTH } from "../../design";

export const PowerUpInsightWrapper = styled(BaseRow)`
  white-space: normal;
  width: ${POWER_UPS_INSIGHT_WIDTH};
`;
