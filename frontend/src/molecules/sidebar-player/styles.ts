import styled from "@emotion/styled";
import { BaseColumn } from "../../atoms";
import { containerHeight } from "../../design";

export const SidebarPlayerWrapper = styled(BaseColumn)`
  height: ${containerHeight.fluid};
`;
