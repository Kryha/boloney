import { param } from "./param";
import { general } from "./general";
import { error } from "./error";
import { loginForm } from "./form";

export const text = {
  param,
  general,
  error,
  loginForm,
};
